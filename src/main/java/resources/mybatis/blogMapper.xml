<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="host.guojing.web.dao.IBlogDao">

	<select id="getBlog" resultType="java.lang.String" parameterType="map" >
		select content from ${table_name} WHERE id = ${id};
	</select>
	

	
</mapper> 
